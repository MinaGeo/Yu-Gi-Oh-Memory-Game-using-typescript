{
  "name": "memory-game",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc --build",
    "start": "npm run build && live-server ."
  },
  "devDependencies": {
    "typescript": "3.9"
  }
}
